% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rcp1armCount.R
\name{plot_rcp1armCount}
\alias{plot_rcp1armCount}
\title{Plot Regional Consistency Probability for Single-Arm MRCT (Count Endpoint)}
\usage{
plot_rcp1armCount(
  lambda = 2,
  lambda0 = 3,
  dispersion = 1,
  PI = 0.5,
  N_vec = c(20, 40, 100),
  J = 3,
  f1_seq = seq(0.1, 0.9, by = 0.1),
  nsim = 10000,
  seed = 1
)
}
\arguments{
\item{lambda}{Numeric scalar. Expected count per patient under the alternative
hypothesis. Must be positive. Default is \code{2}.}

\item{lambda0}{Numeric scalar. Expected count per patient under the historical
control. Must be positive. Default is \code{3}.}

\item{dispersion}{Numeric scalar. Dispersion parameter of the negative binomial
distribution. Must be positive. Default is \code{1}.}

\item{PI}{Numeric scalar. Effect retention threshold for Method 1.
Must be in \eqn{[0, 1]}. Default is \code{0.5}.}

\item{N_vec}{Integer vector. Total sample sizes for each facet column.
Default is \code{c(20, 40, 100)}.}

\item{J}{Positive integer (>= 2). Number of regions. Default is \code{3}.}

\item{f1_seq}{Numeric vector. Sequence of Region 1 allocation proportions.
Each value must be in \eqn{(0, 1)}. Default is \code{seq(0.1, 0.9, by = 0.1)}.}

\item{nsim}{Positive integer. Number of Monte Carlo iterations for simulation.
Default is \code{10000}.}

\item{seed}{Non-negative integer. Random seed for simulation. Default is \code{1}.}
}
\value{
A ggplot2 object.
}
\description{
Generate a faceted plot of Regional Consistency Probability (RCP) as a function
of the regional allocation proportion \eqn{f_1} for count endpoints
(negative binomial model).
Formula and simulation results are shown for Method 1 (log-RR and linear-RR
scales) and Method 2. Facet rows correspond to the two Method 1 scales
(\eqn{\log(RR)} and \eqn{1 - RR}), and facet columns correspond to total
sample sizes specified in \code{N_vec}.

Regional sample sizes are allocated as:
\eqn{N_{j1} = \lfloor N \times f_1 \rfloor} and
\eqn{N_{j2} = \cdots = N_{jJ} = (N - N_{j1}) / (J - 1)}.
}
\examples{
p <- plot_rcp1armCount(
  lambda     = 2,
  lambda0    = 3,
  dispersion = 1,
  PI         = 0.5,
  N_vec      = c(20, 40, 100),
  J          = 3
)
print(p)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rcp1armMilestoneSurvival.R
\name{plot_rcp1armMilestoneSurvival}
\alias{plot_rcp1armMilestoneSurvival}
\title{Plot Regional Consistency Probability for Single-Arm MRCT (Milestone Survival Endpoint)}
\usage{
plot_rcp1armMilestoneSurvival(
  lambda = log(2)/10,
  t_eval = 8,
  S0 = exp(-log(2) * 8/5),
  t_a = 3,
  t_f = 10,
  lambda_dropout = NULL,
  PI = 0.5,
  N_vec = c(20, 40, 100),
  J = 3,
  f1_seq = seq(0.1, 0.9, by = 0.1),
  nsim = 10000,
  seed = 1
)
}
\arguments{
\item{lambda}{Numeric scalar. True hazard rate under the alternative hypothesis.
Must be positive. Default is \code{log(2) / 10}.}

\item{t_eval}{Numeric scalar. Milestone evaluation time point. Must be positive.
Default is \code{8}.}

\item{S0}{Numeric scalar. Historical control survival rate at \code{t_eval}.
Must be in \eqn{(0, 1]}. Default is \code{exp(-log(2) * 8 / 5)}.}

\item{t_a}{Numeric scalar. Accrual period. Must be positive. Default is \code{3}.}

\item{t_f}{Numeric scalar. Follow-up period. Must be positive. Default is \code{10}.}

\item{lambda_dropout}{Numeric scalar or \code{NULL}. Dropout hazard rate.
If \code{NULL} (default), no dropout is assumed.}

\item{PI}{Numeric scalar. Effect retention threshold for Method 1.
Must be in \eqn{[0, 1]}. Default is \code{0.5}.}

\item{N_vec}{Integer vector. Total sample sizes for each facet column.
Default is \code{c(20, 40, 100)}.}

\item{J}{Positive integer (>= 2). Number of regions. Default is \code{3}.}

\item{f1_seq}{Numeric vector. Sequence of Region 1 allocation proportions.
Each value must be in \eqn{(0, 1)}. Default is \code{seq(0.1, 0.9, by = 0.1)}.}

\item{nsim}{Positive integer. Number of Monte Carlo iterations for simulation.
Default is \code{10000}.}

\item{seed}{Non-negative integer. Random seed for simulation. Default is \code{1}.}
}
\value{
A ggplot2 object.
}
\description{
Generate a faceted plot of Regional Consistency Probability (RCP) as a function
of the regional allocation proportion \eqn{f_1} for milestone survival endpoints.
Formula and simulation results are shown together for both Method 1 and Method 2.
Facet columns correspond to total sample sizes specified in \code{N_vec}.

Regional sample sizes are allocated as:
\eqn{N_{j1} = \lfloor N \times f_1 \rfloor} and
\eqn{N_{j2} = \cdots = N_{jJ} = (N - N_{j1}) / (J - 1)}.
}
\examples{
p <- plot_rcp1armMilestoneSurvival(
  lambda = log(2) / 10,
  t_eval = 8,
  S0     = exp(-log(2) * 8 / 5),
  t_a    = 3,
  t_f    = 10,
  PI     = 0.5,
  N_vec  = c(20, 40, 100),
  J      = 3
)
print(p)

}
